version: '3.8'
services:
  api:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
      - ./logs:/app/logs
    ports:
      - 8080:8080
    command: npm run start:dev
    container_name: api

